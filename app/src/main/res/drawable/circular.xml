<?xml version="1.0" encoding="utf-8"?>

    <shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadiusRatio="3"
        android:shape="ring"
        android:thicknessRatio="30"
        android:useLevel="false">
        <size
            android:width="120dp"
            android:height="120dp" />

        <gradient
            android:centerColor="@color/timer_center"
            android:centerY="0.5"
            android:endColor="@color/timer_end"
            android:startColor="@color/timer_start"
            android:type="sweep"
            android:angle="45"
            android:useLevel="false" />
    </shape>
